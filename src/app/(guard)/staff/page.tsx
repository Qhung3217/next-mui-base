import { CONFIG } from 'src/global-config';

// ----------------------------------------------------------------------

export const metadata = { title: `Nhân viên - ${CONFIG.appName}` };

export default function Page() {
  return <h1>Nhân viên</h1>;
}
